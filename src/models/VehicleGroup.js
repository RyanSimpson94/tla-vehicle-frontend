const VehicleGroup = class VehicleGroup {
  constructor(id, vehicles, order) {
    this.id = id;
    this.vehicles = vehicles;
    this.order = order;
  }
};

export default VehicleGroup;
export {
  VehicleGroup
};